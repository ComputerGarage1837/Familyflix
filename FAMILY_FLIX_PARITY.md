# Family Flix Windows parity tracker

This is the release gate for the Windows client. A successful build alone does
not mean it can replace the Android TV app. Each row needs a Windows interaction
check against the same Jellyfin server and user profiles before release.

| Area | Windows source today | Required check or work |
| --- | --- | --- |
| Fixed server URL and visible-user sign in | Native API client and visible-user sign-in screen coded | Verify passwordless/password accounts, profile switch and persisted sign-in |
| Search and Favourites | Native search page, remote-operable alphabet/number keyboard, paginated movie/show/episode results and Jellyfin Favourites action coded; Favourites remain separate from Watchlist | Verify search navigation, result artwork and cross-device Favourites updates |
| Native playback, audio and subtitle tracks, resume | Native libmpv launch, focusable seek bar, audio/subtitle track chooser, playback speed, explicit Resume/Start over, Android-shared resume rewind (0–300 seconds) and picture-fit preference, and session reporting coded; rapid next-item transitions now queue playback reports until the prior item is stopped | Test representative x265 movies, older TV audio, subtitles, resume, seek, speed, fit/crop/stretch, playlist hand-offs and watched-status sync |
| Up Next after an episode | Native next-unwatched-episode lookup and Extended/Minimal/Off end screen coded. Windows reads and edits Android's per-profile 0–30 second countdown, media-queuing switch and Still Watching threshold, and reads and edits Android/Web's per-series `PLAY_NEXT` and `STOP_AFTER_EPISODE` choices while respecting the Kids Mode automatic-next limit | Build and test automatic-next, Still Watching prompt, countdown cancellation, stop-after, season boundaries, multi-episode files, Kids Mode and remote focus against real profiles |
| Home libraries, preferred order, hidden libraries, Continue Watching and Deck | Native Qt home loads libraries and rows; episode cards show series, season/episode code and title; watched/resume badges and progress are visible. All Libraries browses paginated collections with Android-shared sort, Favourites and unwatched filters; per-user library order, rail visibility, Home row order/visibility and item-identity focus restoration use Android's existing shared layout preferences; active-older-season Deck correction is coded | Verify ordering/visibility and browse-filter persistence in both directions, full-library browsing and Deck against watched older seasons on real profiles |
| Personal and family Watchlists | Native personal and family Watchlist read, display, artwork metadata, membership and voting coded; personal Watchlist is a configurable Home row | Test cross-device sync, revision conflicts, artwork appearance and playback auto-removal |
| Mixed movie/show/episode playlists | Native server-backed list, create, create-and-add, rename, paginated browse, add, remove, reorder, Play all and Play from here controls coded; sequential playback and series-to-episode expansion coded | Test large playlists, mixed playback order, automatic next item, rename, and create-and-add flow on real server |
| Profile switch and Watch Together | Native visible-user chooser, saved-profile switching, participant selection, combined label, feed owner, playback reporting, server-synced presets, exit flow and fair merged group Deck coded. Each participant's recent activity now corrects an active older-season card independently after the initial Deck appears | Test login/profile switching, presets, party reporting and Deck against multiple real profiles, including older-season unwatched progress and duplicate-card contributor labels |
| Family Night and household voting | Native picker merges current/Watch Together personal Watchlists, resolves moved items by title and provider ID, filters type/runtime/age/genre, rerolls, opens details or plays a movie/first unwatched show episode; playback resolves current viewer's state first; household voting is available on item details | Test with real multi-profile lists, moved items, ratings, episode lookup and vote sync |
| Kids Mode | Per-profile local settings for enablement, spoiler hiding, bedtime, episode limit and parent PIN coded; profile access requires PIN when Kids Mode is on; playback checks bedtime; session sleep timer and episode-limit guards now cover playlist hand-offs and automatic Next Up | Verify PIN/profile switching, hidden art/text, bedtime, both episode-limit paths and sleep timer on Windows; align remaining Kids Mode settings with Android where they are server-shared |
| Backdrops, themes and detail actions | Native details and artwork selection follow Android's wide-background priority (item backdrop, parent backdrop, wide thumbnails, then wide primary), with focus-driven backgrounds on Home, Search, library browse and Watchlist plus 60-second random idle rotation. Season pages keep their parent show's backdrop, and detail pages use the newly focused item while its full metadata loads. Read-only live API checks confirmed recent episodes have no own backdrop (404) but their parent backdrop exists (200). Show/season/episode navigation and cast, episode numbering, year, runtime, rating, genres, media format, trailer playback/opening, a Go to show action, Play next unwatched show episode, watched/unwatched and Favourite actions, compact single-row actions and 15 palette choices coded. Season lists now have episode thumbnails, watch counts and focus-following remote scroll | Verify artwork on screen when allowed, then port remaining Android detail actions and settings; test remote focus and theme contrast |
| Intro, recap, preview and outro prompts | Native segment fetch, Ask/Auto/Off defaults and playback prompt coded. Windows reads and can edit Android/Web's per-series intro override | Build and runtime test detection, timing, focus and per-series overrides |
| Live TV guide, preview, categories and time shifting | Native categories, channel list, two-hour viewport/scrolling timeline, preview/fullscreen selection coded | Test live playback, guide timing, category/disabled-channel handling, pause/seek and buffer controls |
| Movie/show and Live TV one-hour buffers | Native libmpv buffer logic and separate 5–60 minute settings are wired to a Windows settings screen with free-storage display; memory-first then capped temporary-disk fallback is coded | Test long VOD and live playback, storage cap, buffer cleanup and seek behavior; actual hour depends on bitrate and stream seekability |
| Issues and broken-media warnings | Native item-detail warnings and movie/episode reporting coded against the server plugin | Test reports and warnings; admin Health Centre remains in the Jellyfin dashboard as requested |
| Settings sync and updates | Windows reads the Android profile-preference document and syncs theme, backdrop, clock, Next Up and segment actions through that same record while preserving unrelated Android keys; it also reads and edits the existing per-series audio/subtitle, intro and autoplay choices. Library/Home layout and skip-forward duration use Android's existing user settings; Watch Together presets sync through Jellyfin; native Windows release check prompts only for a newer release containing a Windows installer. Portable INI-format settings are now isolated from installed-app settings | Build and test settings changes in both directions on a disposable profile before release; verify conflict handling, track selection and update persistence; port the other shared Android preferences rather than inventing Windows-only rules. Installer currently opens as a download rather than installing automatically |
| Keyboard, mouse and remote | Native Qt controls and hidden-until-interaction playback bar coded; Home cards now expose a two-column context menu by remote Select hold/Menu key, mouse hold or right-click, with all actions visible | Test D-pad, short/long Select, Back, page restoration and focus on every screen |

The Windows installer should be published only after all checks pass. Until
then the Android TV release remains the complete Family Flix client.
